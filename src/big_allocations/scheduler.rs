// This is just stupid experiment, not going to be in production - if turns out to be successful, can be moved to production

// TODO: Implement scheduler
pub struct Scheduler {
    pub in_progress: bool,
}
